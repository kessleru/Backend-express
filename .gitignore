node_modules/
dist/
build/
coverage/
.nyc_output/
.cache/

# Banco de dados local (SQLite)
data/*.sqlite
data/*.sqlite-journal
data/*.sqlite-wal
data/*.sqlite-shm
data/*.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env files (o .env.example É versionado)
.env
.env.*
!.env.example

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# OS/editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Prisma Client gerado (módulo 10) — recriado por `npx prisma generate`
src/exemplos/10-prisma/gerado/

# Minhas resoluções — espaço pessoal, fora do repositório.
# Ignora as pastas de módulo (01-*, 02-*, …), mas mantém versionados o README,
# o tsconfig e o .gitkeep: sem eles a pasta não chega em quem clona o repo.
/src/playground/*/